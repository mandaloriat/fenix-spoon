"""A third-party adapter whose dependency is not installed.

The commonest real failure by a distance, and the one whose tempting handling — letting the
`ImportError` propagate — takes down a server whose other capabilities are fine.
"""

raise ImportError("No module named 'slepc4py'")
