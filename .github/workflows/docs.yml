name: Docs

# Publishes the mkdocs site to GitHub Pages. Only on main: pull requests get the build
# and the staleness check from ci.yml, which is what catches problems — deploying a
# preview per PR would add moving parts for a site that is a dozen pages.
on:
  push:
    branches: [main]
  workflow_dispatch:

# One deploy at a time, and never cancel one in flight: a half-finished Pages deployment
# is a broken site, unlike a cancelled test run.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: pip install -e "./server[docs]"

      - name: Regenerate the protocol reference
        # Belt and braces: ci.yml already fails a PR that leaves this stale, but the
        # published site should be generated from the models on main regardless of how
        # a commit got there.
        run: python server/tools/generate_protocol_reference.py

      - name: Build
        run: mkdocs build

      - uses: actions/configure-pages@v5
        with:
          # Turn Pages on if it is not already, rather than failing with "Get Pages site
          # failed" until somebody flips a switch in Settings. The first run on main did
          # exactly that: it regenerated the reference and built the site, then fell over
          # at the last step. A workflow that needs an undocumented manual prerequisite is
          # a workflow that is broken for whoever clones this next.
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
