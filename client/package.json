{
  "name": "fenix-spoon-client-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "npm workspace for the Fenix Spoon browser packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @fenix-spoon/client && npm run build --workspace @fenix-spoon/geometry-2d",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "license": "MIT"
}
